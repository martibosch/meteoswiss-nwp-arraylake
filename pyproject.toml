[build-system]
requires = ["uv_build>=0.9.4,<0.10.0"]
build-backend = "uv_build"

[project]
name = "meteoswiss-nwp-arraylake"
version = "0.1.0"
description = "MeteoSwiss NWP Store on ArrayLake-managed icechunk"
authors = [{name = "Martí Bosch", email = "marti.bosch@meteoswiss.ch"}]
readme = "README.md"
license = {text = "GPL-3.0-only"}
classifiers = [
  "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14"
]
requires-python = ">=3.10"

[project.urls]
repository = "https://github.com/martibosch/meteoswiss-nwp-arraylake"

[tool.codespell]
ignore-regex = '^\s*"image\/png":\s.*'
skip = "CHANGELOG.md,pixi.lock"

[tool.pixi.dependencies]
python = "==3.12"
just = ">=1.40.0,<2"
pre-commit = ">=4.4.0,<5"
arraylake = ">=1.3.0,<2"
modal-client = ">=1.3.4,<2"

[tool.pixi.feature.notebook.dependencies]
ipykernel = ">=7.1.0,<8"
jupyterlab = ">=4.0,<5"
papermill = ">=2.5.0,<3"
numpy = ">=1.26.4,<2"
pandas = ">=1.5.0,<3"
matplotlib = ">=3.10.8,<4"
cartopy = ">=0.25.0,<0.26"
netcdf4 = ">=1.7.4,<2"
boto3 = ">=1.42.73,<2"
tqdm = ">=4.67.3,<5"
xarray = ">=2026.2.0,<2027"
arraylake = ">=1.3.0,<2"

[tool.pixi.feature.notebook.pypi-dependencies]
meteodata-lab = ">=0.5.0"

[tool.pixi.environments]
default = { features = [] }
notebook = { features = ["notebook"] }

[tool.pixi.pypi-dependencies]
meteoswiss_nwp_arraylake = {path = ".", editable = true}

[tool.pixi.workspace]
authors = ["Martí Bosch <marti.bosch@meteoswiss.ch>"]
channels = ["conda-forge"]
name = "meteoswiss-nwp-arraylake"
platforms = ["win-64", "linux-64", "osx-64", "osx-arm64"]
version = "0.1.0"

[tool.ruff]
line-length = 88

[tool.ruff.format]
docstring-code-format = true

[tool.ruff.lint]
select = ["D", "E", "F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["meteoswiss_nwp_arraylake"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.uv.build-backend]
module-name = "meteoswiss_nwp_arraylake"
module-root = ""
